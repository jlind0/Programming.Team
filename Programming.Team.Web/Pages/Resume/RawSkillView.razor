@inherits ReactiveComponentBase<RawSkillViewModel>
@if (ViewModel != null)
{   
    <MudChip T="string" OnClose="ViewModel.AddSkill.BindCommand<MudChip<string>>()" 
        CloseIcon="@Icons.Material.Filled.Add">@ViewModel.Name</MudChip>
}